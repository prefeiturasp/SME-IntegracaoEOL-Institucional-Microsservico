"""Configuração da aplicação Unidade Educacional."""

from django.apps import AppConfig


class UnidadeEducacionalConfig(AppConfig):
    """Configura a aplicação Unidade Educacional."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.unidade_educacional'
